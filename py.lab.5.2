s = input("Введите строку: ")
count = s.count(':') # Подсчет вхождений [cite: 11]
new_s = s.replace(':', '%') # Замена шаблона [cite: 11]
print(f"Результат: {new_s}")
print(f"Количество замен: {count}")
