s = input("Введите строку: ")
dots_count = s.count('.') 
# Заменяем точку на "ничего" для удаления [cite: 11, 39]
new_s = s.replace('.', '') 
print(f"Строка без точек: {new_s}")
print(f"Удалено символов: {dots_count}")
