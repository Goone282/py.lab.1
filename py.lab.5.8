s = input("Введите строку с точкой в конце: ")
if s.endswith('.'):
    s = s[:-1] # Срез: убираем последний символ (точку) [cite: 11, 32]
words = s.split()
print(f"Количество слов: {len(words)}") # len() возвращает длину списка слов [cite: 11, 87]
