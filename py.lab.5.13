s = input("Введите строку со скобками: ")
start = s.find('(') # Поиск номера первого вхождения [cite: 11, 37]
end = s.find(')')
if start != -1 and end != -1:
    # Извлечение среза между индексами [cite: 11, 32]
    print(f"Внутри скобок: {s[start+1:end]}")
else:
    print("Скобки не найдены")
